{
  "name": "my-image-decoder",
  "version": "1.0.1",
  "description": "JSI image decoder for React Native + Expo (Android). Decodes PNG/JPEG -> RGBA Uint8Array.",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib",
    "src",
    "android",
    "plugin",
    "app.plugin.js",
    "react-native.config.js",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build"
  },
  "peerDependencies": {
    "react-native": ">=0.71"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@expo/config-plugins": "^8.0.0"
  },
  "react-native": {
    "android": "android"
  }
}
